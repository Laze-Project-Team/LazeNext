関数:コラッツ (整数:入力) => (整数:出力) {
  もし ((入力 % 2) == 0) ならば {
    出力 = 入力 / 2;
  } でなければ {
    出力 = 入力 * 3 + 1;
  }
  表示(出力);
  もし (出力 > 1) ならば {
    整数:返り値 = コラッツ(出力);
  }
}
関数:実行 () => () {
  整数:初期数 = 20;
  表示(初期数);
  整数:返り値 = コラッツ(初期数);
}