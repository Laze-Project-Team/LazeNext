function: main() => (){
	Array<int>: primeNumbers;
	primeNumbers.push(2);
	(int: number = 3;)from(number > 100)to(number = number + 2;){
		int: flag = 0;
		repeat(primeNumbers.length()){
			if((number % primeNumbers.get(counter)) == 0){
				flag = 1;
			}
		}
		if(flag == 0){
			primeNumbers.push(number);
		}
	}
	repeat(primeNumbers.length()){
		print(primeNumbers.get(counter));
	}
}
